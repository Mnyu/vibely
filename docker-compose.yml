services:
  vibely-db:
    image: postgres:latest
    container_name: vibely-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    volumes:
      - postgresql:/var/lib/postgresql

volumes:
  postgresql:
